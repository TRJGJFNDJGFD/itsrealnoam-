# Public base URL of the legendil-status-api deployment (no trailing
# slash needed). This is NOT a secret — the API token stays only on the
# Velocity plugin and is never read by this website.
VITE_STATUS_API_URL=http://localhost:3001
