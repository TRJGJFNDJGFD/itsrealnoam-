// Legend-IL ships with an original generative voxel-scene system (see
// src/components/PixelScene.tsx) instead of stock photography, so the site
// never depends on external or third-party Minecraft imagery.
//
// Drop real renders/screenshots into /public/images/ and reference them here
// to swap any scene for a photo — every component below falls back to the
// generative scene automatically when a path is left empty.
export const IMAGES = {
  hero: "",
  skymines: "",
  lifesteal: "",
  practice: "",
  community: "",
};
